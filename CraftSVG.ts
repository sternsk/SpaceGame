export abstract class CraftSVG{
    
    public static getGElement(type: string): SVGGElement{
        switch(type){
            case `rocket`:
                
        
        }
        let defaultG = document.createElementNS("http://www.w3.org/2000/svg","g")
        return (defaultG)
    }
}